// uma. Asegúrate de tipar adecuadamente los parámetros y el valor de retorno. 6. Tipado en funciones
// Crea una función greet que tome un argumento name de tipo string y devuelva una
// frase de saludo usando ese nombre. 7. Clase básica
function greet(name: string): string {
    return `hola, ${name}!`;
}
console.log(greet("Nazaa")); // llama a la función y pasar el valor de la variable name