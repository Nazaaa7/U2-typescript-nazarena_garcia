// Escribe una función add que acepte dos números como argumentos y devuelva su
// suma. Asegúrate de tipar adecuadamente los parámetros y el valor de retorno. 

function add(num1: number, num2: number): number {
    return num1 + num2;
}

let num1: number = 5;
let num2: number = 10;
console.log("La suma de los numeros es de: ",add(num1, num2)); //llamar a la función y pasarle las variable con numeros como parametro